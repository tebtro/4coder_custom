#if !defined(SPACE_MODE_H)


CUSTOM_ID(command_map, mapid_space_mode); // main space mode

CUSTOM_ID(command_map, mapid_space_mode_chord_b); // buffer
CUSTOM_ID(command_map, mapid_space_mode_chord_f); // file
CUSTOM_ID(command_map, mapid_space_mode_chord_p); // project
CUSTOM_ID(command_map, mapid_space_mode_chord_q); // quit
CUSTOM_ID(command_map, mapid_space_mode_chord_w); // window


#define SPACE_MODE_H
#endif
